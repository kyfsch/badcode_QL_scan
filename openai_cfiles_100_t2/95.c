
void configureTPID(uint16_t outerTPID, uint16_t innerTPID) {
    // Configure the outer TPID
    // Write outerTPID to the desired register
    
    // Configure the inner TPID
    // Write innerTPID to the desired register
}


In this function, the `outerTPID` and `innerTPID` parameters represent the values to be configured for the outer and inner TPIDs respectively. You can replace the comments with the specific code to write the TPID values to the desired registers in your embedded system.