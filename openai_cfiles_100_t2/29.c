
typedef enum {
    CPUFWD,
    CPUFWD2,
    CPUFWD3,
    PARFWDCPU,
    DSPFWDCPU
} VlanOamMode;

void setVlanOamMode(VlanOamMode mode)
{
    // code to set the mode of VLAN OAM packet
    // based on the provided mode argument
}
