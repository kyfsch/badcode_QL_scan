/// This function is used to read the JECED ID of the Flash device connected to SPI.</s>.
int spi_nor_read_id(void)
\n
      8 + 4 args in compound statement
      int chip_id = *addr++ & 0xff;
105   ;
\D
4
4
107   return (chip_id );
}
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

  __xio__mem_read
(
      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      8 + 21 args in compound statement
      __xio__spi_xfer
(
      .
      args: 
      stmts:    IF_IC ("SPI NOR read ID") { .

      .